// import everything and name it utils
import * as utils from "./utils/index.mjs";

console.log(utils.capitalize("meow!"));
console.log(`Rolling one 20-sided die! You rolled: ${utils.diceRoll(20, 1)}`);
